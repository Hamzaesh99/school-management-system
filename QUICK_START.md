# ⚡ دليل البدء السريع - Quick Start Guide

## 🚀 خطوات التشغيل (5 دقائق)

### الخطوة 1️⃣: تأكد من تثبيت المتطلبات

تحقق من تثبيت:
- ✅ **Node.js** (v14 أو أحدث)
- ✅ **MariaDB/MySQL** (قيد التشغيل)

---

### الخطوة 2️⃣: إعداد قاعدة البيانات

#### أ. افتح MariaDB/MySQL من Command Prompt:
```bash
mysql -u root -p
```

#### ب. أنشئ قاعدة البيانات:
```sql
CREATE DATABASE school_management_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
exit;
```

#### ج. استورد البيانات:
```bash
mysql -u root -p school_management_db < database\schema.sql
```

---

### الخطوة 3️⃣: تحديث ملف `.env`

افتح ملف `.env` وضع كلمة مرور قاعدة البيانات الخاصة بك:

```env
DB_PASSWORD=YOUR_PASSWORD_HERE
```

احفظ الملف!

---

### الخطوة 4️⃣: تشغيل الخادم

**الطريقة 1 - باستخدام ملف البدء:**
```bash
start.bat
```

**الطريقة 2 - باستخدام npm:**
```bash
npm start
```

**الطريقة 3 - وضع التطوير:**
```bash
npm run dev
```

---

### الخطوة 5️⃣: افتح المتصفح

انتقل إلى:
```
http://localhost:3000
```

---

## 🔐 تسجيل الدخول

### بيانات المدير:
```
Username: admin
Password: admin123
```

---

## ✅ تم! النظام يعمل الآن

استمتع باستخدام نظام إدارة المدرسة! 🎓

---

## ⚠️ إذا واجهت مشكلة:

### المشكلة: خطأ في الاتصال بقاعدة البيانات
**الحل:**
1. تأكد من تشغيل MariaDB/MySQL
2. تحقق من كلمة المرور في `.env`
3. تأكد من إنشاء قاعدة البيانات

### المشكلة: Port 3000 مستخدم
**الحل:**
غيّر المنفذ في `.env`:
```env
PORT=3001
```

### المشكلة: لا تظهر الصفحات
**الحل:**
1. تأكد من وجود مجلد `public/`
2. تحقق من Console في المتصفح (F12)
3. أعد تشغيل الخادم

---

## 📖 للمزيد من المعلومات:
- اقرأ `USER_GUIDE.md` للدليل الشامل
- اقرأ `README.md` للتفاصيل التقنية
- اقرأ `PROJECT_SUMMARY.md` لملخص المشروع
