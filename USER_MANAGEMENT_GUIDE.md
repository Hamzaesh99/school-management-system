# نظام إدارة المستخدمين

## نظرة عامة
تم إنشاء نظام كامل لإدارة المستخدمين حيث يمكن للمدير فقط إضافة وتعديل وحذف حسابات المعلمين والطلاب وأولياء الأمور.

## الميزات الرئيسية

### 🔐 الأمان
- ✅ الوصول محدود للمدير فقط
- ✅ جميع الـ API endpoints محمية بـ JWT authentication
- ✅ تشفير كلمات المرور باستخدام bcrypt
- ✅ التحقق من الصلاحيات في كل عملية

### 📋 الوظائف المتاحة
1. **عرض جميع المستخدمين**
   - قائمة كاملة بجميع المستخدمين
   - عرض الإحصائيات حسب الدور
   
2. **إضافة مستخدم جديد**
   - معلم (Teacher)
   - طالب (Student)
   - ولي أمر (Parent)
   - مدير (Admin)

3. **تعديل بيانات المستخدم**
   - الاسم والبريد الإلكتروني
   - الدور والهاتف
   - العنوان وتاريخ الميلاد
   - الحالة (نشط/غير نشط)

4. **تغيير كلمة المرور**
   - يمكن للمدير تغيير كلمة مرور أي مستخدم

5. **حذف المستخدم**
   - حذف دائم من النظام
   - مع رسالة تأكيد

## الوصول للنظام

### رابط الصفحة
```
http://localhost:3000/users-management
```

### بيانات الدخول (للمدير فقط)
```
Username: admin
Password: admin123
```

## API Endpoints

| Method | Endpoint | الوصف |
|--------|----------|-------|
| GET | `/api/users` | جلب جميع المستخدمين |
| GET | `/api/users/stats` | جلب الإحصائيات |
| GET | `/api/users/:id` | جلب مستخدم واحد |
| POST | `/api/users` | إضافة مستخدم جديد |
| PUT | `/api/users/:id` | تحديث بيانات مستخدم |
| PUT | `/api/users/:id/password` | تغيير كلمة المرور |
| DELETE | `/api/users/:id` | حذف مستخدم |

## مثال على إضافة مستخدم

### Request
```json
POST /api/users
Headers: {
  "Authorization": "Bearer <token>",
  "Content-Type": "application/json"
}
Body: {
  "username": "ahmed123",
  "email": "ahmed@school.com",
  "password": "password123",
  "full_name": "أحمد محمود",
  "role": "teacher",
  "phone": "0501234567",
  "gender": "male"
}
```

### Response
```json
{
  "success": true,
  "message": "تم إضافة المستخدم بنجاح",
  "data": {
    "user_id": 5,
    "username": "ahmed123",
    "email": "ahmed@school.com",
    "full_name": "أحمد محمود",
    "role": "teacher"
  }
}
```

## الملفات المنشأة

### Backend
- `controllers/user.controller.js` - المنطق الخاص بإدارة المستخدمين
- `routes/user.routes.js` - مسارات الـ API

### Frontend
- `public/users-management.html` - صفحة إدارة المستخدمين
- `public/js/users-management.js` - منطق الـ frontend

## ملاحظات مهمة

1. **الأمان أولاً**
   - لا يمكن للمدير حذف حسابه الخاص
   - كل عملية تتطلب token صالح
   - التحقق من الصلاحيات في كل request

2. **التحقق من البيانات**
   - اسم المستخدم والبريد الإلكتروني يجب أن يكونا فريدين
   - كلمة المرور يجب أن تكون 6 أحرف على الأقل
   - جميع الحقول المطلوبة يتم التحقق منها

3. **واجهة المستخدم**
   - تصميم احترافي وسهل الاستخدام
   - رسائل واضحة للنجاح والخطأ
   - modals للإضافة والتعديل

## استخدام النظام

1. **سجّل الدخول كمدير**
   ```
   http://localhost:3000/login
   Username: admin
   Password: admin123
   ```

2. **انتقل لصفحة إدارة المستخدمين**
   ```
   http://localhost:3000/users-management
   ```

3. **أضف مستخدم جديد**
   - اضغط على زر "إضافة مستخدم جديد"
   - املأ البيانات المطلوبة
   - اختر الدور المناسب
   - احفظ

4. **تعديل مستخدم**
   - اضغط على زر "تعديل" في صف المستخدم
   - عدّل البيانات
   - احفظ

5. **تغيير كلمة المرور**
   - اضغط على زر "كلمة المرور"
   - أدخل كلمة المرور الجديدة
   - احفظ

6. **حذف مستخدم**
   - اضغط على زر "حذف"
   - أكد الحذف

## المميزات الإضافية

✅ الإحصائيات الفورية (عدد المستخدمين لكل دور)
✅ البحث والفرز في الجدول
✅ واجهة سريعة الاستجابة
✅ تصميم متوافق مع جميع الأجهزة
✅ رسائل تأكيد قبل الحذف
✅ التحديث التلقائي بعد كل عملية

---

**ملاحظة:** هذا النظام مصمم للحفاظ على مصداقية المعلومات من خلال السماح للمدير فقط بإدارة المستخدمين.
