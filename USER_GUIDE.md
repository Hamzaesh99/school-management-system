# دليل الاستخدام - نظام إدارة المدرسة الإلكتروني
## School Management System User Guide

---

## 📚 المحتويات

1. [التثبيت والإعداد](#التثبيت-والإعداد)
2. [تشغيل النظام](#تشغيل-النظام)
3. [بيانات الدخول](#بيانات-الدخول)
4. [الشاشات الرئيسية](#الشاشات-الرئيسية)
5. [الأدوار والصلاحيات](#الأدوار-والصلاحيات)
6. [استكشاف الأخطاء](#استكشاف-الأخطاء)

---

## 🚀 التثبيت والإعداد

### المتطلبات الأساسية:
- ✅ Node.js (v14 أو أحدث)
- ✅ MariaDB أو MySQL (v10.5 أو أحدث)
- ✅ متصفح الويب (Chrome, Firefox, Edge)

### خطوات التثبيت:

#### 1. تثبيت التبعيات
```bash
npm install
```

#### 2. إعداد قاعدة البيانات

##### أ. إنشاء قاعدة البيانات
افتح MariaDB/MySQL وقم بتنفيذ:
```sql
CREATE DATABASE school_management_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

##### ب. استيراد البيانات
```bash
# استخدم واحدة من الطرق التالية:

# الطريقة 1: من سطر الأوامر
mysql -u root -p school_management_db < database/schema.sql

# الطريقة 2: من داخل MySQL
USE school_management_db;
SOURCE database/schema.sql;
```

#### 3. تكوين متغيرات البيئة

ملف `.env` موجود بالفعل. قم بتحديث البيانات حسب إعداداتك:

```env
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=YOUR_PASSWORD_HERE
DB_NAME=school_management_db
DB_PORT=3306

PORT=3000

JWT_SECRET=school_management_secret_key_2024

NODE_ENV=development
```

---

## ▶️ تشغيل النظام

### وضع التطوير (Development):
```bash
npm run dev
```

### وضع الإنتاج (Production):
```bash
npm start
```

### الوصول للنظام:
افتح المتصفح وانتقل إلى:
```
http://localhost:3000
```

---

## 👤 بيانات الدخول الافتراضية

### 1. المدير (Administrator)
- **اسم المستخدم:** `admin`
- **كلمة المرور:** `admin123`
- **الصلاحيات:** جميع الصلاحيات

### 2. المعلم (Teacher)
- **اسم المستخدم:** `teacher1`
- **كلمة المرور:** `admin123`
- **الصلاحيات:** تسجيل الحضور، إدخال الدرجات، عرض الطلاب

### 3. الطالب (Student)
- **اسم المستخدم:** `student1`
- **كلمة المرور:** `admin123`
- **الصلاحيات:** عرض الجدول، الدرجات، الحضور

### 4. ولي الأمر (Parent)
- **اسم المستخدم:** `parent1`
- **كلمة المرور:** `admin123`
- **الصلاحيات:** عرض أداء الأبناء فقط

> ⚠️ **تنبيه أمني:** يجب تغيير كلمات المرور بعد أول تسجيل دخول!

---

## 📱 الشاشات الرئيسية

### 1. 🏠 الصفحة الرئيسية (`/`)
- عرض تقديمي للنظام
- المميزات الرئيسية
- رابط تسجيل الدخول

### 2. 🔐 تسجيل الدخول (`/login`)
- إدخال اسم المستخدم وكلمة المرور
- التحقق والمصادقة
- تحويل المستخدم حسب دوره

### 3. 📊 لوحة التحكم (`/dashboard`)
- **للمدير:**
  - إحصائيات شاملة (عدد الطلاب، المعلمين، الصفوف، المواد)
  - الإعلانات الأخيرة
  - الرسائل الجديدة

- **للمعلم:**
  - عدد الصفوف والطلاب
  - الجدول الدراسي
  - الإعلانات

- **للطالب:**
  - المعدل التراكمي
  - نسبة الحضور
  - الإعلانات

### 4. 👨‍🎓 إدارة الطلاب (`/students`)
- عرض قائمة الطلاب
- إضافة طالب جديد
- تعديل بيانات طالب
- حذف طالب
- عرض تفاصيل الطالب (الدرجات، الحضور)

### 5. 👨‍🏫 إدارة المعلمين (`/teachers`)
- عرض قائمة المعلمين
- إضافة معلم جديد
- تعديل بيانات المعلم
- عرض التخصصات والصفوف

### 6. 👥 إدارة أولياء الأمور (`/parents`)
- عرض قائمة أولياء الأمور
- ربط ولي الأمر بالطلاب
- منح صلاحيات المتابعة

### 7. 🏫 الصفوف والمواد (`/classes`)
- **الصفوف:**
  - عرض جميع الصفوف
  - إضافة صف جديد
  - تحديد معلم الصف

- **المواد:**
  - عرض جميع المواد
  - إضافة مادة جديدة
  - تحديد الساعات المعتمدة

### 8. 📅 الجداول الدراسية (`/schedules`)
- عرض الجدول الأسبوعي
- تخصيص حسب الصف
- تحديد الفصل الدراسي
- عرض المعلم والقاعة

### 9. ✅ الحضور والغياب (`/attendance`)
- تسجيل الحضور اليومي
- عرض السجلات
- حالات: (حاضر، غائب، متأخر، معذور)
- تصفية حسب التاريخ والصف

### 10. 📝 الدرجات (`/grades`)
- إضافة درجات جديدة
- أنواع الامتحانات:
  - منتصف الفصل
  - نهائي
  - اختبار قصير
  - واجب
  - عملي
- عرض النسب المئوية
- تصفية حسب الطالب/الصف/المادة

### 11. 📋 عرض ولي الأمر (`/parent-view`)
- المعدل العام للطالب
- نسبة الحضور
- الدرجات الأخيرة
- سجل الحضور اليومي

---

## 🔐 الأدوار والصلاحيات

### المدير (Admin)
✅ إدارة المستخدمين (جميع الأدوار)
✅ إدارة الطلاب
✅ إدارة المعلمين
✅ إدارة الصفوف والمواد
✅ إنشاء الجداول الدراسية
✅ عرض جميع التقارير
✅ نشر الإعلانات
✅ إرسال الرسائل

### المعلم (Teacher)
✅ عرض الطلاب في صفوفه
✅ تسجيل الحضور
✅ إدخال الدرجات
✅ عرض جدوله الدراسي
✅ إرسال رسائل للطلاب وأولياء الأمور
❌ إضافة/حذف مستخدمين

### الطالب (Student)
✅ عرض جدوله الدراسي
✅ عرض درجاته
✅ عرض سجل الحضور
✅ قراءة الإعلانات
❌ تعديل أي بيانات

### ولي الأمر (Parent)
✅ عرض أداء أبنائه
✅ عرض الدرجات
✅ عرض الحضور
✅ قراءة الإعلانات
❌ تعديل أي بيانات

---

## 🔧 استكشاف الأخطاء

### المشكلة: لا يمكن الاتصال بقاعدة البيانات
**الحل:**
1. تحقق من تشغيل MariaDB/MySQL
2. تأكد من صحة بيانات الاتصال في `.env`
3. تحقق من صلاحيات المستخدم في قاعدة البيانات

```sql
GRANT ALL PRIVILEGES ON school_management_db.* TO 'root'@'localhost';
FLUSH PRIVILEGES;
```

### المشكلة: خطأ 404 - الصفحة غير موجودة
**الحل:**
1. تأكد من تشغيل الخادم
2. تحقق من المنفذ في `.env`
3. استخدم `http://localhost:3000` وليس `localhost:3000`

### المشكلة: فشل تسجيل الدخول
**الحل:**
1. تحقق من استيراد بيانات schema.sql
2. استخدم بيانات الدخول الافتراضية
3. تحقق من console في المتصفح للأخطاء

### المشكلة: الصفحات لا تحمل CSS/JavaScript
**الحل:**
1. تحقق من مجلد `public/` ومحتوياته
2. افتح Developer Tools في المتصفح
3. تحقق من Console للأخطاء

---

## 📞 الدعم الفني

للمساعدة أو الإبلاغ عن مشاكل:
- افتح Developer Console (F12) وانسخ الأخطاء
- تحقق من ملفات الـ logs
- راجع ملف README.md

---

## ✨ نصائح للاستخدام الأمثل

1. **الأمان:**
   - غيّر كلمات المرور الافتراضية فوراً
   - استخدم كلمات مرور قوية
   - حدّث JWT_SECRET في `.env`

2. **الأداء:**
   - استخدم وضع الإنتاج للأداء الأفضل
   - قم بعمل نسخة احتياطية لقاعدة البيانات بانتظام

3. **البيانات:**
   - أدخل بيانات حقيقية تدريجياً
   - استخدم رموز موحدة للصفوف والمواد
   - حافظ على تحديث البيانات

4. **التطوير:**
   - استخدم `npm run dev` للتطوير
   - راجع logs عند حدوث أخطاء
   - اختبر التغييرات قبل النشر

---

**🎓 نتمنى لك تجربة استخدام ممتعة ومثمرة!**
