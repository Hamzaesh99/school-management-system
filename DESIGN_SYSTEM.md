# 🎨 الهوية البصرية والنظام التصميمي
# منصة التواصل والعلوم الإلكترونية للمدرسة

## 📘 نظرة عامة

تم تصميم الهوية البصرية للمنصة بناءً على مبادئ التصميم الحديث والعصري، مع التركيز على سهولة الاستخدام والتجربة البصرية الممتعة.

---

## 🎨 لوحة الألوان الأساسية

### 1. البرتقالي (اللون الرئيسي)
**الاستخدام**: التركيز، الأزرار الأساسية، العناصر التفاعلية

- **البرتقالي الأساسي**: `#F47920`
  - RGB: `rgb(244, 121, 32)`
  - يستخدم للعناصر الأساسية والتفاعلية

- **البرتقالي الفاتح**: `#FF9B4D`
  - للحالات المحومة (hover states)
  
- **البرتقالي الأفتح**: `#FFB878`
  - للخلفيات الخفيفة
  
- **البرتقالي الداكن**: `#D96810`
  - للظلال والتدرجات
  
- **البرتقالي الأغمق**: `#BE5500`
  - للحالات النشطة (active states)

### 2. الأزرق الداكن (اللون الثانوي)
**الاستخدام**: العناوين، الأزرار الثانوية، الخلفيات الاحترافية

- **الأزرق الأساسي**: `#0B1171`
  - RGB: `rgb(11, 17, 113)`
  - يستخدم للعناوين والعناصر الثانوية

- **الأزرق الفاتح**: `#1A2898`
  - للحالات المحومة
  
- **الأزرق الأفتح**: `#2940BF`
  - للخلفيات الفاتحة
  
- **الأزرق الداكن**: `#060B4D`
  - للتناقضات القوية
  
- **الأزرق الأغمق**: `#030529`
  - للنصوص والتفاصيل الدقيقة

### 3. الألوان المحايدة

- **الأبيض**: `#FFFFFF` - الخلفيات الرئيسية
- **الأسود**: `#000000` - النصوص الأساسية

**تدرجات الرمادي**:
- `--gray-50`: `#F9FAFB` - خلفيات فاتحة جداً
- `--gray-100`: `#F3F4F6` - خلفيات فاتحة
- `--gray-200`: `#E5E7EB` - حدود فاتحة
- `--gray-300`: `#D1D5DB` - حدود
- `--gray-400`: `#9CA3AF` - نصوص ثانوية
- `--gray-500`: `#6B7280` - نصوص معتدلة
- `--gray-600`: `#4B5563` - نصوص داكنة
- `--gray-700`: `#374151` - نصوص أكثر قتامة
- `--gray-800`: `#1F2937` - نصوص شبه سوداء
- `--gray-900`: `#111827` - نصوص سوداء

### 4. ألوان الحالات

- **النجاح (Success)**: `#10B981` - للنجاحات والإشعارات الإيجابية
- **التحذير (Warning)**: `#F59E0B` - للتحذيرات
- **الخطأ (Error)**: `#EF4444` - للأخطاء والرسائل السلبية
- **معلومات (Info)**: `#3B82F6` - للمعلومات العامة

---

## 🌈 التدرجات (Gradients)

### التدرج الأساسي (البرتقالي)
```css
background: linear-gradient(135deg, #F47920 0%, #D96810 100%);
```
**الاستخدام**: الأزرار الأساسية، العناوين المميزة

### التدرج الثانوي (الأزرق)
```css
background: linear-gradient(135deg, #0B1171 0%, #1A2898 100%);
```
**الاستخدام**: الأزرار الثانوية، الخلفيات الاحترافية

### التدرج المختلط
```css
background: linear-gradient(135deg, #F47920 0%, #0B1171 100%);
```
**الاستخدام**: العناصر الخاصة، البطاقات المميزة

### تدرج البطل (Hero)
```css
background: linear-gradient(135deg, 
  rgba(11, 17, 113, 0.95) 0%, 
  rgba(11, 17, 113, 0.85) 50%, 
  rgba(244, 121, 32, 0.9) 100%);
```
**الاستخدام**: الصفحة الرئيسية، الأقسام الكبيرة

---

## 🎭 الظلال (Shadows)

### الظلال الأساسية
```css
--shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
--shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
--shadow-md: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
--shadow-lg: 0 20px 25px -5px rgba(0, 0, 0, 0.1);
--shadow-xl: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
```

### الظلال الملونة
```css
--shadow-orange: 0 10px 30px -5px rgba(244, 121, 32, 0.35);
--shadow-blue: 0 10px 30px -5px rgba(11, 17, 113, 0.35);
```

---

## 📐 الأحجام والمسافات

### الحواف الدائرية (Border Radius)
```css
--radius-sm: 6px;      /* صغير */
--radius: 10px;        /* عادي */
--radius-md: 12px;     /* متوسط */
--radius-lg: 16px;     /* كبير */
--radius-xl: 20px;     /* كبير جداً */
--radius-2xl: 24px;    /* ضخم */
--radius-full: 9999px; /* دائري كامل */
```

### المسافات (Spacing)
```css
--spacing-xs: 0.25rem;  /* 4px */
--spacing-sm: 0.5rem;   /* 8px */
--spacing: 1rem;        /* 16px */
--spacing-md: 1.5rem;   /* 24px */
--spacing-lg: 2rem;     /* 32px */
--spacing-xl: 3rem;     /* 48px */
--spacing-2xl: 4rem;    /* 64px */
```

---

## ✍️ الخطوط (Typography)

### العائلة الخطية
```css
font-family: 'Cairo', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
```

### أحجام الخطوط
```css
--font-size-xs: 0.75rem;    /* 12px */
--font-size-sm: 0.875rem;   /* 14px */
--font-size-base: 1rem;     /* 16px */
--font-size-lg: 1.125rem;   /* 18px */
--font-size-xl: 1.25rem;    /* 20px */
--font-size-2xl: 1.5rem;    /* 24px */
--font-size-3xl: 1.875rem;  /* 30px */
--font-size-4xl: 2.25rem;   /* 36px */
--font-size-5xl: 3rem;      /* 48px */
```

### أوزان الخطوط
- **عادي (Regular)**: 400
- **متوسط (Medium)**: 500
- **نصف سميك (Semibold)**: 600
- **سميك (Bold)**: 700
- **سميك جداً (Extrabold)**: 800

---

## 🎬 الرسوم المتحركة (Animations)

### الانتقالات (Transitions)
```css
--transition-fast: 150ms ease-in-out;
--transition-base: 300ms ease-in-out;
--transition-slow: 500ms ease-in-out;
```

### التأثيرات المتحركة

**ظهور تدريجي (Fade In)**:
```css
@keyframes fadeIn {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
}
```

**انزلاق من اليمين**:
```css
@keyframes slideIn {
    from { transform: translateX(-100%); opacity: 0; }
    to { transform: translateX(0); opacity: 1; }
}
```

**النبض (Pulse)**:
```css
@keyframes pulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.05); }
}
```

**التوهج (Glow)**:
```css
@keyframes glow {
    0%, 100% { box-shadow: 0 0 20px rgba(244, 121, 32, 0.3); }
    50% { box-shadow: 0 0 40px rgba(244, 121, 32, 0.6); }
}
```

---

## 🧩 المكونات (Components)

### الأزرار (Buttons)

**الأزرار الأساسية**:
- `.btn-primary` - برتقالي مع تدرج
- `.btn-secondary` - أزرق مع تدرج
- `.btn-outline-primary` - حدود برتقالية
- `.btn-outline-secondary` - حدود زرقاء

### البطاقات (Cards)

خصائص البطاقة الأساسية:
- خلفية بيضاء
- حواف دائرية كبيرة (20px)
- ظل متوسط
- حد علوي ملون بالتدرج المختلط
- تأثير رفع عند التمرير

### الجداول (Tables)

- رأس بتدرج مختلط (برتقالي-أزرق)
- صفوف متناوبة بخلفية فاتحة عند التمرير
- حواف دائرية على الجدول الكامل

### الشارات (Badges)

- `.badge-primary` - برتقالي فاتح
- `.badge-secondary` - أزرق فاتح
- `.badge-success` - أخضر فاتح
- `.badge-warning` - أصفر فاتح
- `.badge-danger` - أحمر فاتح

---

## 📱 الاستجابة (Responsive Design)

### نقاط التوقف (Breakpoints)

```css
/* شاشات صغيرة جداً */
@media (max-width: 640px) { }

/* أجهزة لوحية */
@media (max-width: 768px) { }

/* أجهزة لوحية كبيرة */
@media (max-width: 1024px) { }

/* أجهزة سطح المكتب */
@media (min-width: 1280px) { }
```

---

## 🎯 إرشادات الاستخدام

### متى تستخدم اللون البرتقالي؟
- الأزرار الأساسية (Call-to-Action)
- الروابط الهامة
- العناصر التفاعلية الأساسية
- التأكيدات والإشعارات الإيجابية

### متى تستخدم اللون الأزرق؟
- العناوين والنصوص المهمة
- الأزرار الثانوية
- الخلفيات الاحترافية
- العناصر الثابتة والمعلوماتية

### متى تستخدم التدرج المختلط؟
- رؤوس الصفحات الرئيسية
- البطاقات المميزة
- العناصر الخاصة والفريدة
- رؤوس الجداول

---

## 🔧 أمثلة عملية

### زر أساسي
```html
<button class="btn btn-primary">
    التسجيل الآن
</button>
```

### بطاقة مع عنوان
```html
<div class="card fade-in">
    <div class="card-header">
        <h3 class="card-title">إحصائيات النظام</h3>
    </div>
    <div class="card-body">
        <!-- المحتوى -->
    </div>
</div>
```

### شارة نجاح
```html
<span class="badge badge-success">مكتمل</span>
```

### تنبيه معلومات
```html
<div class="alert alert-info">
    تم حفظ التغييرات بنجاح!
</div>
```

---

## 📚 الملفات ذات الصلة

- `/public/css/style.css` - الأنماط الأساسية
- `/public/css/theme.css` - نظام الألوان والتصميم
- `/public/css/dashboard.css` - أنماط لوحة التحكم

---

## 📞 الدعم والمساعدة

للمزيد من المعلومات حول استخدام النظام التصميمي، يرجى الرجوع إلى:
- توثيق المكونات الكامل
- دليل المطور
- أمثلة التطبيق

---

**آخر تحديث**: 2025-12-22
**الإصدار**: 2.0
**الحالة**: نشط ✅
