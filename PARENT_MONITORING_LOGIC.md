# منطق نظام متابعة ولي الأمر (Parent Monitoring Logic)

هذا المستند يشرح المنطق البرمجي وهيكلة البيانات التي تمكن ولي الأمر من متابعة درجات أبنائه، ومعرفة معلميهم، وفصولهم الدراسية داخل نظام "تواصل".

## 1. هيكلة قاعدة البيانات (Database Structure)

يعتمد النظام على العلاقات المترابطة بين الجداول التالية لربط ولي الأمر بأبنائه وبياناتهم الدراسية:

### الجداول الأساسية:
1.  **`users`**: يحتوي على بيانات جميع المستخدمين (آباء، طلاب، معلمين).
2.  **`students`**: جدول تفصيلي للطلاب، يربط حساب الطالب (`user_id`) بحساب ولي الأمر (`parent_id`) وبالصف الدراسي (`class_id`).
3.  **`classes`**: يحتوي على معلومات الصفوف الدراسية.
4.  **`class_subjects`**: يربط الصفوف بالمواد والمعلمين (أي معلم يدرس أي مادة لأي صف).
5.  **`grades`**: يحتوي على درجات الطلاب في المواد المختلفة.

### العلاقات (Relationships):
*   **ولي الأمر -> الأبناء**: علاقة واحد لمتعدد (One-to-Many). ولي الأمر الواحد (في جدول `users`) يمكن أن يكون لديه عدة أبناء في جدول `students` عبر حقل `parent_id`.
*   **الطالب -> الصف**: الطالب ينتمي لصف واحد (`class_id` في جدول `students`).
*   **الصف -> المعلمين**: الصف يرتبط بمواد، وكل مادة لها معلم معين عبر جدول `class_subjects`.
*   **الطالب -> الدرجات**: الطالب لديه سجلات درجات متعددة في جدول `grades`.

---

## 2. المنطق البرمجي لاسترجاع البيانات (Logic Flow)

عندما يقوم ولي الأمر بتسجيل الدخول ويرغب في عرض بيانات أبنائه، يتم تنفيذ الخطوات التالية:

### الخطوة 1: تحديد الأبناء
نبحث في جدول `students` عن جميع السجلات التي يكون فيها `parent_id` مطابقاً لمعرف المستخدم الحالي (ولي الأمر).

```sql
SELECT * FROM students WHERE parent_id = [ID_ولي_الأمر];
```

### الخطوة 2: جلب بيانات الصف والمدرسة (لأي ابن)
من نتيجة الخطوة السابقة، نستخدم `class_id` لكل طالب لجلب اسم الصف من جدول `classes`.

```sql
SELECT class_name, academic_year FROM classes WHERE class_id = [ID_صف_الطالب];
```

### الخطوة 3: معرفة المعلمين والمواد
لنعرف من هم معلمي هذا الطالب، ننظر إلى صفه الدراسي. جدول `class_subjects` يخبرنا بالمواد المقررة على هذا الصف ومن هو المعلم لكل مادة.

```sql
SELECT 
    s.subject_name AS المادة,
    u.full_name AS المعلم
FROM class_subjects cs
JOIN subjects s ON cs.subject_id = s.subject_id
JOIN users u ON cs.teacher_id = u.user_id
WHERE cs.class_id = [ID_صف_الطالب];
```

### الخطوة 4: جلب الدرجات
نبحث في جدول `grades` باستخدام `student_id` الخاص بالابن لجلب درجاته وتواريخ الاختبارات.

```sql
SELECT 
    s.subject_name,
    g.exam_type,
    g.grade,
    g.max_grade
FROM grades g
JOIN class_subjects cs ON g.class_subject_id = cs.class_subject_id
JOIN subjects s ON cs.subject_id = s.subject_id
WHERE g.student_id = [ID_الطالب];
```

---

## 3. مثال عملي للبيانات (Data walkthrough)

لنفترض أن لدينا:
*   **ولي الأمر**: "علي حسن" (ID: 10)
*   **الابن**: "أحمد علي" (Student ID: 50, ينتمي للصف "الأول أ" الذي معرفه Class ID: 1)

**كيف يرى الأب المعلمين؟**
1.  النظام يرى أن "أحمد" في الصف "1".
2.  النظام يذهب لجدول ربط المواد (`class_subjects`) ويبحث عن الصف "1".
3.  يجد أن:
    *   مادة "الرياضيات" يدرسها المعلم "أستاذ محمد".
    *   مادة "العلوم" يدرسها المعلم "أستاذ خالد".
4.  يعرض النظام هذه القائمة لولي الأمر.

**كيف يرى الأب الدرجات؟**
1.  النظام يبحث في جدول الدرجات عن الطالب رقم "50".
2.  يجد درجة "18/20" في "امتحان منتصف الفصل" لمادة الرياضيات.
3.  يعرضها في لوحة تحكم ولي الأمر.

---

## 4. التنفيذ في الكود (Implementation Hint)

في المتحكمات (Controllers)، يفضل إنشاء دالة مخصصة لولي الأمر، مثلاً `getParentDashboardStats`، تقوم بتجميع هذه البيانات في كائن واحد (JSON Object) وإرسالها للواجهة الأمامية:

```json
{
  "children": [
    {
      "name": "أحمد علي",
      "class": "الصف الأول أ",
      "teachers": ["أستاذ محمد (رياضيات)", "أستاذ خالد (علوم)"],
      "recent_grades": [
        {"subject": "الرياضيات", "score": 18, "total": 20},
        {"subject": "العلوم", "score": 15, "total": 20}
      ]
    }
  ]
}
```

---

## 5. إضافات مقترحة: المستوى المنطقي (Logical Additions - Important)

لجعل النظام أكثر واقعية ومرونة، يمكن إضافة التحسينات التالية على بنية البيانات:

### 1️⃣ دعم أكثر من ولي أمر للطالب (More flexible relations)
حالياً، الطالب مرتبط بحقل `parent_id` واحد. الأفضل هو إنشاء جدول وسيط لتمكين ربط الطالب بأكثر من ولي أمر (أب، أم، وصي).

*   **الجدول المقترح**: `student_parents`
    *   `student_id`
    *   `parent_id`
    *   `relation_type` (قيم: 'أب'، 'أم'، 'وصي')
*   **الفائدة**: يعكس الواقع الاجتماعي ويزيد من احترافية النظام (قابل للتوسعة مستقبلاً).

### 2️⃣ تحديد الفصل الدراسي (Semester tracking)
في جدول الدرجات `grades`، يضاف:
*   `semester` (الفصل الأول / الثاني)
*   `academic_year` (السنة الدراسية)
*   **الفائدة**: يتيح لولي الأمر فلترة الدرجات حسب الفصل الدراسي، وهو أمر جوهري للعملية التربوية.

### 3️⃣ منطق الصلاحيات (Authorization Logic)
يجب تطبيق قواعد أمان صارمة في الـ Backend:
*   **ولي الأمر**: يرى *فقط* البيانات المرتبطة بـ IDs أبنائه.
*   **المعلم**: يرى *فقط* الطلاب المسجلين في الصفوف التي يدرسها.

---

## 6. إضافات مقترحة: المستوى الوظيفي (High Value Features)

هذه الإضافات ترفع من قيمة النظام بشكل كبير للمستخدم النهائي:

### 4️⃣ نظام الإشعارات (Notifications)
إضافة جدول `notifications` لتنبيه ولي الأمر فورا.
*   **الحقول**: `user_id`, `title`, `body`, `is_read`, `created_at`.
*   **أمثلة**: "تم رصد درجة جديدة لابنك"، "غياب الطالب اليوم"، "ملاحظة سلوكية من المعلم".

### 5️⃣ سجل الغياب (Attendance)
إضافة جدول `attendance`.
*   **الحقول**: `student_id`, `date`, `status` (حاضر/غائب/متأخر), `note`.
*   **الفائدة**: متابعة المواظبة تعتبر تربوياً أهم أحياناً من متابعة الدرجات.

### 6️⃣ ملاحظات المعلم (Teacher Notes)
إضافة جدول `teacher_notes` للتواصل النوعي.
*   **الحقول**: `student_id`, `teacher_id`, `note`, `created_at`.
*   **أمثلة**: "الطالب أبدى تحسناً ملحوظاً"، "يرجى متابعة الواجبات المنزلية".

---

## 7. إضافات مقترحة: المستوى التقني (Technical Optimizations)

### 7️⃣ تحسين الأداء (Performance with Views)
بدلاً من الاستعلامات المعقدة المتكررة، يمكن إنشاء **Database View** يجمع بيانات الطلاب وأولياء أمورهم وصفوفهم.
*   مثال: `CREATE VIEW parent_students_overview AS ...`

### 8️⃣ واجهة برمجية موحدة (Unified Parent API)
بدلاً من استدعاء عدة روابط (endpoints) لجلب البيانات، يتم إنشاء نقطة واحدة فعالة لولي الأمر:
*   **الرابط**: `GET /api/parent/dashboard`
*   **الاستجابة**: تعيد هيكلاً كاملاً يحتوي على (قائمة الأبناء، صفوفهم، أسماء المعلمين، آخر الدرجات، وآخر الإشعارات) في طلب واحد. هذا يعكس تصميماً احترافياً للـ API.

